---
title: "Divorce Prediction"
author: "Elouan-Joris"
date: "2025-09-30"
output:
  html_document:
    toc: true                # ✅ active la table des matières
    toc_float: true          # ✅ menu déroulant flottant sur le côté
    number_sections: true    # ✅ numérotation des sections
    theme: cosmo             # 🌈 thème visuel (cosmo, united, journal, etc.)
    highlight: tango         # 🎨 style pour le code
    code_folding: show       # 🔽 possibilité de plier/déplier le code
---


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# - Introduction

## Observations des données brute

```{r 1 Chargement du fichier}
data.brute = read.csv("divorce_df.csv")
str(data.brute)
```
## Analyse des données brutes

**Presentation des variables**

Variables principales :

- `age_at_marriage` = L'âge à laquelle le couple s'est marié (int)
- `marriage_duration_years` = Temps écoulé à partir du mariage (int)
- `divorced` = Si le couple est divorcé ou non (int)

Variables métadonnées :

- `num_children` = Nombre d'enfant dans le couple
- `education_level` = Le niveau d'éducation
- `employment_status` = Statut du couple
- `combined_income` = Revenu combiné
- `religious_compatibility` = Compatibilité religieuse
- `cultural_background_match` = Correspondance culturelle
- `communication_score` = Score de communication
- `conflict_frequency` = Fréquence des conflits
- `conflict_resolution_style` = Style de résolution des conflits
- `mental_health_issues` = Problèmes de santé mentale
- `financial_stress_level` = Niveau de stress financier
- `infidelity_occurred` = Infidélité survenue
- `counseling_attended` = A suivi un counseling
- `social_support` = Soutien social
- `shared_hobbies_count` = Nombre de hobbies partagés
- `marriage_type` = Type de mariage
- `pre_marital_cohabitation` = Cohabitation avant le mariage
- `domestic_violence_history` = Historique de violence domestique
- `trust_score` = Score de confiance


```{r 2 Boxplot Numérique}
barplot(table(data.brute$age_at_marriage) / sum(table(data.brute$age_at_marriage)))

```

```{r 3 Boxplot Numérique}
# Nom des colonnes
named_columns <- colnames(data.brute)
type_columns <- sapply(data.brute, class)


for (name in named_columns) {
  if (type_columns[name] == "integer"){
  boxplot(data.brute[[name]], main = name)
  }
}

```


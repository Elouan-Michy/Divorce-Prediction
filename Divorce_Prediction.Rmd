---
title: "Divorce Prediction"
author: "Elouan-Joris"
date: "2025-09-30"
output:
  html_document:
    toc: true                # ✅ active la table des matières
    toc_float: true          # ✅ menu déroulant flottant sur le côté
    number_sections: true    # ✅ numérotation des sections
    theme: cosmo             # 🌈 thème visuel (cosmo, united, journal, etc.)
    highlight: tango         # 🎨 style pour le code
    code_folding: show       # 🔽 possibilité de plier/déplier le code
    df_print: default
  
---


```{r setup, echo = FALSE}
knitr::opts_chunk$set(
	echo = TRUE,
	message = FALSE,
	warning = FALSE
)
```

```{r package, echo=FALSE, message=FALSE, warning=FALSE, paged.print=FALSE}
library(knitr)
library(kableExtra)
library(dplyr)

```

# - Introduction

```{r 1 Chargement du fichier divorce_df.csv, echo = FALSE}
data.brute <- read.csv("divorce_df.csv")
dimension <- dim(data.brute)
n <- dimension[1]
p <- dimension[2]
```
## - Presentation des variables


```{r echo = FALSE}

# Création du dataframe

Nom_de_la_variable <- c("age_at_marriage","marriage_duration_years","divorced","num_children",
                         "education_level","employment_status","combined_income",
                         "religious_compatibility","cultural_background_match","communication_score",
                         "conflict_frequency","conflict_resolution_style","mental_health_issues",
                         "financial_stress_level","infidelity_occurred","counseling_attended",
                         "social_support","shared_hobbies_count","marriage_type",
                         "pre_marital_cohabitation","domestic_violence_history","trust_score")

Description <- c("Âge au mariage","Durée du mariage","Divorce (oui/non)","Nombre d’enfants",
                  "Niveau d’éducation","Statut professionnel","Revenu combiné",
                  "Compatibilité religieuse","Correspondance culturelle","Score de communication",
                  "Fréquence des conflits","Style de résolution de conflit","Problèmes de santé mentale",
                  "Niveau de stress financier","Infidélité survenue","A suivi un counseling",
                  "Soutien social","Nombre de hobbies partagés","Type de mariage",
                  "Cohabitation avant mariage","Historique de violence domestique","Score de confiance")

Type <- c("Quantitative","Quantitative","Qualitative","Quantitative","Qualitative",
           "Qualitative","Quantitative","Qualitative","Qualitative","Quantitative",
           "Quantitative","Qualitative","Qualitative","Quantitative","Qualitative",
           "Qualitative","Quantitative","Quantitative","Qualitative","Qualitative",
           "Qualitative","Quantitative")

Sous_type = c("Discrète","Discrète","Binaire","Discrète","Ordinale",
                "Nominale","Continue","Nominale","Binaire","Continue",
                "Discrète","Nominale","Binaire","Continue","Binaire",
                "Binaire","Continue","Discrète","Nominale","Binaire",
                "Binaire","Continue")

variables <- data.frame(
  Nom_de_la_variable,
  Description,
  Type,
  Sous_type,
  stringsAsFactors = FALSE
)

# Fonction de coloration conditionnelle
variables <- variables %>%
  mutate(
    Type = cell_spec(Type,
                     background = ifelse(Type=="Quantitative","#5DADE2","#58D68D"),
                     color="black", bold=TRUE, escape=FALSE),
    Sous_type = cell_spec(Sous_type,
                          background = case_when(
                            Sous_type == "Binaire"   ~ "#A3E4D7",  # vert menthe
                            Sous_type == "Discrète"  ~ "#85C1E9",  # bleu clair
                            Sous_type == "Ordinale"  ~ "#F7DC6F",  # jaune doré
                            Sous_type == "Nominale"  ~ "#F5B041",  # orange doux
                            Sous_type == "Continue"  ~ "#E59866"   # rouge rosé
                          ),
                          color = "black",
                          escape = FALSE)
  )

# Affichage du tableau
kable(variables, "html", escape=FALSE, caption="📊 Tableau des variables avec code couleur") %>%
  kable_styling(bootstrap_options = c("striped","hover","condensed"), full_width=FALSE) %>%
  column_spec(1, bold=TRUE, color="black", background="#D6EAF8") %>%    # colonne Nom_de_la_variable
  column_spec(2, width="8cm", bold=TRUE, background="#EBF5FB") %>%       # colonne Description
  row_spec(0, bold=TRUE, color="white", background="#2C3E50")            # header
```

## - Boxplot des données Quantitative


```{r 2 Boxplot Numérique, echo = FALSE, fig.align='center'}
# Prendre que les variables quantitatives
vars_quanti <- Nom_de_la_variable[Type == "Quantitative"]

for (i in 0:4) {
  # Grille 2 lignes × 5 colonnes
  par(mfrow = c(1, 2), mar = c(4, 4, 3, 1))
  for (j in 1:2) {
    var_name <- vars_quanti[2*i+j]
    boxplot(
      data.brute[[var_name]],
      main = var_name,
      col = "#5DADE2",
      boxwex = 0.5,
      outline = TRUE,
      cex.main = 0.9,
      cex.axis = 0.9,
      las = 1,
      frame.plot = FALSE
    )
    grid(nx = NA, ny = NULL, col = "lightgray", lty = "dotted")
    }
}
par(mfrow = c(1, 1))



```

## - Boxplot des données Qualitatives

```{r 3 Barplot qualitative, echo = FALSE, fig.align='center'}
# Sélection des variables qualitatives
vars_quali <- Nom_de_la_variable[Type == "Qualitative"]

# Grille 2 lignes × 5 colonnes
par(mfrow = c(2, 6), mar = c(6, 4, 3, 1)) 

for (i in 0:5) {
  # Grille 2 lignes × 5 colonnes
  par(mfrow = c(1, 2), mar = c(6, 4, 3, 1))
  for (j in 1:2) {
  var_name <- vars_quali[2*i+j]
  # Comptage des modalités
  counts <- table(data.brute[[var_name]])
  
  # Titre avec retour à la ligne si besoin
  main_title <- gsub("_", "\n", var_name)
  
  barplot(
    counts,
    main = main_title,
    col = "#58D68D",
    border = "gray25",
    cex.main = 0.9,
    cex.axis = 0.8,
    cex.names = 0.7,       # taille des noms des modalités
    las = 2,               # rotation des labels pour qu’ils soient lisibles
    frame.plot = FALSE
  )
  grid(nx = NA, ny = NULL, col = "lightgray", lty = "dotted")
  }
}


# ✅ Revenir à une seule figure
par(mfrow = c(1, 1))
```

# - Fonction de survie

**Estimateur de la fonction de survie sans données de censure :**

$$
\hat{S}(t) = \sum^{n}_{i = 1}1_{t_i>t}
$$

- n = le nombre total d'observation
- ti = temps jusqu'à l'évènement pour l'individu i
- $1_{t_i>t}$ = Indicateur qui vaut 1 si l'individu n'a pas encore eu l'évènement à t, 0 sinon.

**Pour introduire la censure, on définit (ici le divorce) :**

$$
\delta_i =
\begin{cases}
1 & \text{si l'événement observé pour i} \\
0 & \text{si censuré}
\end{cases}
$$
On note $T_{1} \le T_{2} \le \dots \le T_{n}$ les temps triés et $\delta_i$ les évènements correspondants.

Alors l'estimateur de Kaplan-Meier s'écrit :

$$
\hat{S}(t) = \prod_{T_i \le t,\delta_i = 1}(1-\frac{1}{\sum^{n}_{j = 1}1_{T_j \ge T_i}})
$$


```{r 4 Fonction de survie, echo = FALSE, fig.align='center'}
## Donnée NON CENSURE
# Données durée de mariage
mar_dur <- data.brute$marriage_duration_years

n_dim = length(mar_dur)
# Durée 
t_values <- seq(0, max(mar_dur), by = 1)


S_estimate <- sapply(t_values, function(t) {
  sum(mar_dur > t) / n_dim
})

## Donnée CENSURÉ
time <- data.brute$marriage_duration_years
divorce <- data.brute$divorced  # 1 = divorce, 0 = censuré
# Trier par temps
sorted_idx <- order(time)
time_sorted <- time[sorted_idx]
divorce_sorted <- divorce[sorted_idx]
n <- length(time_sorted)
S <- numeric(n)       # stocke la survie à chaque temps
surv_prob <- 1         # survie initiale = 1

for(i in 1:n){
  if(divorce_sorted[i] == 1){
    # nombre à risque : tous les individus >= temps actuel
    n_risk <- sum(time_sorted >= time_sorted[i])
    # mise à jour survie
    surv_prob <- surv_prob * (1 - 1 / n_risk)
  }
  S[i] <- surv_prob
}





# Plot initial vide avec limites
plot(NA, xlim = c(0, max(t_values)), ylim = c(0,1),
     xlab = "Durée du mariage (années)",
     ylab = "S(t) = Probabilité de rester marié",
     main = "Comparaison fonction de survie")

# Ajouter l'escalier
lines(t_values, S_estimate, type = "s", col = "blue", lwd = 2)

lines(time_sorted, S, type = "s", col = "red", lwd = 2)

# Légende
legend("topright", legend = c("Sans censure", "Avec censure"),
       col = c("blue", "red"), lwd = 2)
```


